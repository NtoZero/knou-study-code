export const pseudocodes: Record<string, string[]> = {
  selection: [
    'SelectionSort(A[], n)',
    '  for i ← 0 to n-2',
    '    min ← i',
    '    for j ← i+1 to n-1',
    '      if A[j] < A[min]',
    '        min ← j',
    '    A[i] ↔ A[min]',
  ],
  bubble: [
    'Advanced_BubbleSort(A[], n)',
    '  for (i=0; i < n-1; i++)',
    '    Sorted ← TRUE',
    '    for (j=0; j < (n-1)-i; j++)',
    '      if (A[j] > A[j+1])',
    '        A[j]와 A[j+1]의 자리바꿈',
    '        Sorted ← FALSE',
    '    if (Sorted == TRUE) break',
  ],
  insertion: [
    'InsertionSort(A[], n)',
    '  for (i=1; i < n; i++)',
    '    val ← A[i]',
    '    for (j=i; j > 0 && A[j-1] > val; j--)',
    '      A[j] ← A[j-1]',
    '    A[j] ← val',
  ],
  shell: [
    'ShellSort(A[], n)',
    '  for (D=⌊n/2⌋; D ≥ 1; D=⌊D/2⌋)',
    '    for (i=D; i < n; i++)',
    '      val ← A[i]',
    '      for (j=i; j ≥ D && A[j-D] > val; j=j-D)',
    '        A[j] ← A[j-D]',
    '      A[j] ← val',
  ],
  quick: [
    'QuickSort(A[], n)',
    '  if (n > 1)',
    '    pivot = Partition(A[0..n-1], n)',
    '    QuickSort(A[0..pivot-1], pivot)',
    '    QuickSort(A[pivot+1..n-1], n-pivot-1)',
    '',
    'Partition(A[], n)',
    '  Left ← 1;  Right ← n-1',
    '  while (Left < Right)',
    '    while (Left < n && A[Left] < A[0]) Left++',
    '    while (Right > 0 && A[Right] ≥ A[0]) Right--',
    '    if (Left < Right)',
    '      A[Left]와 A[Right]의 위치 교환',
    '    else',
    '      피벗 A[0]와 A[Right]의 위치 교환',
    '  return (Right)',
  ],
  merge: [
    'MergeSort(A[], n)',
    '  if (n > 1)',
    '    Mid ← ⌊n/2⌋',
    '    B[0..Mid-1] ← MergeSort(A[0..Mid-1], Mid)',
    '    C[0..n-Mid-1] ← MergeSort(A[Mid..n-1], n-Mid)',
    '    A[0..n-1] ← Merge(B, C, Mid, n-Mid)',
    '',
    'Merge(B[], C[], n, m)',
    '  i = j = k = 0',
    '  while (i < n && j < m)',
    '    if (B[i] ≤ C[j])  A[k++] ← B[i++]',
    '    else  A[k++] ← C[j++]',
    '  for (; i < n; i++)  A[k++] ← B[i]',
    '  for (; j < m; j++)  A[k++] ← C[j]',
    '  return A[0..n+m-1]',
  ],
  heap: [
    'HeapSort(A[], n)',
    '  // 단계1: 새 노드의 삽입',
    '  for (i=0; i < n; i++)',
    '    par ← ⌈i/2⌉-1',
    '    while (par ≥ 0 && A[par] < A[i])',
    '      A[par]과 A[i]의 교환',
    '      i ← par;  par ← ⌊(i-1)/2⌋',
    '  // 단계2: 최댓값 삭제 + 힙 재구성',
    '  for (i=n-1; i > 0; i--)',
    '    A[0]와 A[i]의 교환',
    '    cur ← 0;  lch ← 1;  rch ← 2',
    '    do',
    '      if (rch < i && A[lch] < A[rch]) lch ← rch',
    '      if (A[lch] > A[cur])',
    '        A[cur]과 A[lch]의 교환',
    '        cur ← lch',
    '        lch ← cur*2+1;  rch ← cur*2+2',
    '      else lch ← i',
    '    while (lch < i)',
  ],
  counting: [
    'CountingSort(A[1..n], n)',
    '  MIN ← MAX ← A[1]',
    '  for (i=2; i ≤ n; i++)',
    '    if (A[i] < MIN) MIN ← A[i]',
    '    if (A[i] > MAX) MAX ← A[i]',
    '  for (j=MIN; j ≤ MAX; j++)  COUNT[j] ← 0',
    '  for (i=1; i ≤ n; i++)  COUNT[A[i]]++',
    '  for (j=MIN+1; j ≤ MAX; j++)',
    '    COUNT[j] ← COUNT[j] + COUNT[j-1]',
    '  for (i=n; i > 0; i--)',
    '    B[COUNT[A[i]]] ← A[i]',
    '    COUNT[A[i]]--',
    '  return B',
  ],
  radix: [
    'RadixSort(A[], n)',
    '  for (i=1; i ≤ d; i++)',
    '    각 데이터의 i자리의 값에 대해서',
    '    안정적인 정렬 알고리즘 적용',
    '  return A',
  ],
  bucket: [
    'BucketSort(A[], n)',
    '  buckets[0..n-1] ← empty',
    '  max ← max(A)',
    '  for i ← 0 to n-1',
    '    idx ← A[i] * n / (max+1)',
    '    buckets[idx].append(A[i])',
    '  for each bucket',
    '    InsertionSort(bucket)',
    '  A ← concatenate(buckets)',
  ],
  'sequential-search': [
    'SequentialSearch(A[], n, x)',
    '  i = 0;',
    '  while (i < n && A[i] != x)',
    '    i = i + 1;',
    '  return (i);',
  ],
  'binary-search': [
    'BinarySearch(A[], key, Left, Right)',
    '  if (Left > Right) return (-1);',
    '  Mid = (Left + Right) / 2;',
    '  if (A[Mid] == key) return (Mid);',
    '  else if (key < A[Mid])',
    '    BinarySearch(A, key, Left, Mid-1);',
    '  else',
    '    BinarySearch(A, key, Mid+1, Right);',
  ],
  bst: [
    'BST_Search(T, key)',
    '  CNode = T의 루트 노드;',
    '  while (CNode != NULL)',
    '    if (key == CNode.key) return (CNode);',
    '    else if (key < CNode.key)',
    '      CNode = CNode.left;',
    '    else CNode = CNode.right;',
    '  return (NULL);',
    '',
    'BST_Insert(T, x)',
    '  CNode = T의 루트 노드;',
    '  do {',
    '    if (x == CNode.key) return (T);',
    '    else {',
    '      PNode = CNode;',
    '      if (x < CNode.key) CNode = CNode.left;',
    '      else CNode = CNode.right;',
    '    }',
    '  } while (CNode != NULL);',
    '  NewNode.key = x;',
    '  if (x < PNode.key) PNode.left = NewNode;',
    '  else PNode.right = NewNode;',
    '  return (T);',
  ],
  'tree-234': [
    'TwoThreeFour_Insert(T, x)',
    '  루트부터 시작',
    '  현재 노드가 4-노드이면',
    '    중간 키를 부모로 올리고 분할',
    '  x가 들어갈 리프까지 이동',
    '  리프에 x 삽입',
    '',
    'TwoThreeFour_Search(T, x)',
    '  현재 노드에서 x를 탐색',
    '  x가 현재 노드의 키에 있으면 반환',
    '  적절한 자식으로 이동',
    '  null이면 탐색 실패',
  ],
  'red-black-tree': [
    'RB_Insert(T, x)',
    '  BST 방식으로 x 삽입 (빨강)',
    '  while x ≠ root and x.parent가 빨강',
    '    if 삼촌이 빨강',
    '      부모, 삼촌 → 검정, 조부모 → 빨강',
    '    else',
    '      필요시 회전 (LL/RR/LR/RL)',
    '      부모 → 검정, 조부모 → 빨강',
    '  root → 검정',
  ],
  'b-tree': [
    'BT_Insert(T, x)',
    '  루트부터 시작',
    '  현재 노드가 가득 차면',
    '    중간 키를 부모로 올리고 분할',
    '  x가 들어갈 위치까지 이동',
    '  리프에 x 삽입 (정렬 유지)',
  ],
  'hash-table': [
    'HashInsert(T, x)',
    '  h ← hash(x)',
    '  // 개방 해싱',
    '  T[h]의 체인에 x 추가',
    '  // 폐쇄 해싱 - 선형 탐사',
    '  while T[h] ≠ empty',
    '    h ← (h + step) mod m',
    '  T[h] ← x',
  ],
  dfs: [
    'DepthFirstSearch(G, s)',
    '  Push(Stack, s);',
    '  while (Stack != NULL) {',
    '    c = Stack의 top에 있는 정점;',
    '    c.visited = TRUE;',
    '    정점 c를 방문 정점으로 출력;',
    '    do {',
    '      for (v ← c의 모든 인접한 정점)',
    '        if (v.visited == FALSE)',
    '          Push(Stack, v) 후 while문으로;',
    '      c = Pop(Stack);',
    '    } while (Stack != NULL);',
    '  }',
  ],
  bfs: [
    'BreadthFirstSearch(G, s)',
    '  Enqueue(Queue, s);',
    '  s.flag = TRUE;',
    '  while (Queue != NULL) {',
    '    c = Dequeue(Queue);',
    '    정점 c를 방문 정점으로 출력;',
    '    for (v ← c의 모든 인접한 정점)',
    '      if (v.flag == FALSE) {',
    '        Enqueue(Queue, v);',
    '        v.flag = TRUE;',
    '      }',
    '  }',
  ],
  'topological-sort': [
    'TopologicalSort(G)',
    '  n = |V| - 1;',
    '  while (미탐색 정점 s 존재) {',
    '    Push(Stack, s);',
    '    while (Stack != NULL) {',
    '      c = Stack의 top; c.visited = TRUE;',
    '      do {',
    '        for (v ← c의 모든 인접한 정점)',
    '          if (v.visited == FALSE)',
    '            Push(Stack, v) 후 while문으로;',
    '        A[n--] = Pop(Stack);',
    '        c = Stack의 top;',
    '      } while (Stack != NULL);',
    '    }',
    '  }',
    '  return (A);',
  ],
  'connected-components': [
    'ConnectedComponents(G)',
    '  comp ← 0;',
    '  for (각 정점 v) {',
    '    if (v가 미방문) {',
    '      comp ← comp + 1;',
    '      DFS/BFS(v)로 연결된 정점 탐색;',
    '      탐색한 정점들을 comp에 추가;',
    '    }',
    '  }',
  ],
  kruskal: [
    'Kruskal(G)',
    '  T = ∅;',
    '  for (G의 각 정점 v에 대해)',
    '    정점 v로 구성된 연결 성분 초기화;',
    '  가중치가 증가하는 순으로 간선 정렬;',
    '  for (가장 작은 간선부터 모든 간선 (u,v)∈E)',
    '    if (u와 v가 다른 연결 성분) {',
    '      T = T ∪ {(u, v)};',
    '      u, v의 연결 성분을 합침;',
    '    }',
    '    else 간선 (u, v)를 버림;',
    '  return (T);',
  ],
  prim: [
    'Prim(G)',
    '  T = ∅;',
    '  S = {1};',
    '  while (S != V) {',
    '    u∈S, v∈V-S 중 최소 간선 (u,v) 선택;',
    '    T = T ∪ {(u, v)};',
    '    S = S ∪ {v};',
    '  }',
    '  return (T);',
  ],
  dijkstra: [
    'Dijkstra(G, s)',
    '  S = {}; d[s] = 0;',
    '  for (모든 정점 v∈V) {',
    '    d[v] = ∞; prev[v] = NULL;',
    '  }',
    '  while (S != V) {',
    '    d[u]가 최소인 정점 u∈V-S를 선택;',
    '    S = S ∪ {u};',
    '    for (u에 인접한 모든 정점 v)',
    '      if (d[v] > d[u] + W(u, v)) {',
    '        d[v] = d[u] + W(u, v);',
    '        prev[v] = u;',
    '      }',
    '  }',
    '  return (d[], prev[]);',
  ],
  'bellman-ford': [
    'Bellman_Ford(G, s)',
    '  for (모든 정점 v∈V) {',
    '    d[v] = ∞; prev[v] = NULL;',
    '  }',
    '  d[s] = 0;',
    '  for (i = 1; i < n; i++)',
    '    for (모든 간선 (u, v)∈E)',
    '      if (d[v] > d[u] + W(u, v)) {',
    '        d[v] = d[u] + W(u, v);',
    '        prev[v] = u;',
    '      }',
    '  return (d[], prev[]);',
  ],
  'floyd-warshall': [
    'Floyd(G)',
    '  for (i = 1; i <= n; i++)',
    '    for (j = 1; j <= n; j++)',
    '      D[i][j] = A[i][j];',
    '  for (k = 1; k <= n; k++)',
    '    for (i = 1; i <= n; i++)',
    '      for (j = 1; j <= n; j++)',
    '        if (D[i][j] > D[i][k] + D[k][j])',
    '          D[i][j] = D[i][k] + D[k][j];',
    '  return (D[][]);',
  ],
  'ford-fulkerson': [
    'Ford_Fulkerson(N)',
    '  모든 간선의 플로 f를 0으로 초기화;',
    '  while (증가 경로 p가 존재) {',
    '    Δ = min{r(u,v) | p의 <u,v>};',
    '    for (p상의 모든 간선 <u,v>)',
    '      if (<u,v>가 순방향 간선)',
    '        f(u,v) += Δ;',
    '      else f(u,v) -= Δ;',
    '  }',
    '  F = Σ f(w, t);',
    '  return (F);',
  ],
};
