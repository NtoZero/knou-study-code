export interface AlgorithmGuide {
  concept: string;
  steps: string[];
  keyPoints: string[];
}

export const algorithmGuides: Record<string, AlgorithmGuide> = {
  selection: {
    concept:
      '입력 배열에서 가장 작은 값부터 순서대로 "선택"해서 나열하는 방식. 미정렬 부분 A[i..n-1]에서 최솟값을 찾아 A[i]와 위치 교환.',
    steps: [
      'i = 0부터 n-2까지 반복',
      '미정렬 부분 A[i..n-1]에서 최솟값의 인덱스 min 찾기',
      'A[i]와 A[min]의 자리바꿈',
    ],
    keyPoints: [
      '입력 순서에 무관, 항상 O(n²)',
      '불안정적 정렬 (동일 값의 순서 미보장)',
      '제자리 정렬 (추가 공간 상수 개)',
    ],
  },
  bubble: {
    concept:
      '인접한 두 데이터를 차례대로 비교하여 왼쪽이 더 크면 오른쪽과 자리바꿈. 한 패스마다 가장 큰 값이 끝으로 이동.',
    steps: [
      'i = 0부터 n-2까지 반복',
      'j = 0부터 (n-1)-i까지 인접 원소 비교',
      'A[j] > A[j+1]이면 자리바꿈',
      '한 패스에서 교환 없으면 조기 종료 (개선 버전)',
    ],
    keyPoints: [
      '최선 O(n) — 이미 정렬된 경우',
      '최악 O(n²) — 역순 정렬',
      '안정적 정렬, 제자리 정렬',
    ],
  },
  insertion: {
    concept:
      '정렬된 부분에 새 원소를 적절한 위치에 삽입하는 방식. A[0..k-1]이 정렬 부분, A[k]를 뽑아 제자리에 삽입.',
    steps: [
      'i = 1부터 n-1까지 반복',
      'val = A[i] (미정렬 부분의 첫 번째 데이터)',
      '정렬 부분에서 val보다 큰 값들을 한 칸씩 뒤로 이동',
      '빈 자리에 val 삽입',
    ],
    keyPoints: [
      '거의 정렬된 입력에서 가장 효율적',
      '최선 O(n), 최악 O(n²)',
      '안정적 정렬, 제자리 정렬',
    ],
  },
  shell: {
    concept:
      '삽입 정렬의 단점(한 번에 한 자리씩만 이동) 보완. 일정 간격(gap)으로 떨어진 원소들끼리 삽입 정렬하며 간격을 점차 줄임.',
    steps: [
      'D = n/2부터 시작, 매번 D = D/2로 감소',
      '간격 D만큼 떨어진 원소들로 부분배열 구성',
      '각 부분배열에 대해 삽입 정렬 수행',
      'D = 1이 되면 전체 배열에 삽입 정렬 (마지막 패스)',
    ],
    keyPoints: [
      '순열(gap sequence)에 따라 성능 변동',
      '최선 O(n log n) ~ 최악 O(n²)',
      '불안정적 정렬, 제자리 정렬',
    ],
  },
  quick: {
    concept:
      '피벗을 기준으로 배열을 두 부분배열로 분할하고 각각에 순환적으로 퀵 정렬을 적용하는 분할정복 알고리즘.',
    steps: [
      '피벗 선택 (보통 첫 번째 원소)',
      'Partition: Left→에서 피벗보다 큰 값, Right←에서 피벗보다 작은 값을 찾아 교환',
      'Left와 Right가 교차하면 피벗을 제자리에 배치',
      '왼쪽·오른쪽 부분배열에 대해 순환 호출',
    ],
    keyPoints: [
      '평균 O(n log n), 최악 O(n²) — 이미 정렬된 입력',
      '불안정적, 제자리 정렬',
      '피벗 선택의 임의성이 보장되면 평균 성능 유지',
    ],
  },
  merge: {
    concept:
      '배열을 동일한 크기의 두 부분배열로 분할하고, 각각 정렬한 후 합병(merge)하여 하나의 정렬된 배열을 생성하는 분할정복 알고리즘.',
    steps: [
      '배열을 중앙 기준으로 균등 분할',
      '왼쪽·오른쪽 부분배열에 순환적으로 합병 정렬 적용',
      'Merge: 정렬된 두 배열의 원소를 비교하여 작은 값부터 결과 배열에 복사',
      '한쪽이 소진되면 나머지를 그대로 복사',
    ],
    keyPoints: [
      '최선/최악/평균 모두 O(n log n)',
      '안정적 정렬',
      '제자리 아님 — n만큼의 추가 공간 필요',
    ],
  },
  heap: {
    concept:
      '최대 힙(완전 이진 트리에서 부모 >= 자식)을 구성한 후, 루트(최댓값)와 마지막 원소를 교환하며 힙 크기를 줄여 정렬.',
    steps: [
      '입력 배열을 최대 힙으로 변환 (삽입 반복 또는 아래→위 조정)',
      '루트 A[0](최댓값)과 마지막 원소 A[i] 교환',
      '힙 크기를 1 줄이고 힙 재구성 (루트부터 아래로 내려가며 조정)',
      'i = n-1부터 1까지 반복',
    ],
    keyPoints: [
      '최선/최악/평균 모두 O(n log n)',
      '불안정적 정렬',
      '제자리 정렬 — 추가 공간 상수 개',
    ],
  },
  counting: {
    concept:
      '각 값의 출현 빈도를 세어 누적합으로 정렬 위치를 계산하는 비교 기반이 아닌 정렬. 입력값이 작은 정수 범위 내에 있을 때 적용.',
    steps: [
      '입력값의 범위(MIN~MAX) 계산',
      'COUNT 배열에 각 값의 출현횟수 기록',
      'COUNT 배열의 누적합 계산 → 각 값의 마지막 위치',
      '입력 배열을 역순으로 순회하며 결과 배열 B에 배치',
    ],
    keyPoints: [
      'O(n+k), k = 입력값 범위',
      'k가 O(n)이면 선형 시간',
      '안정적 정렬, 제자리 아님',
    ],
  },
  radix: {
    concept:
      '입력값을 자릿수별로 구분하여 낮은 자리부터(LSD) 안정적인 정렬을 반복 적용하는 방식. 각 자릿수에 계수 정렬 등을 사용.',
    steps: [
      '최대 자릿수 d 결정',
      'i = 1(일의 자리)부터 d까지 반복',
      'i번째 자릿수 값을 기준으로 안정적 정렬(계수 정렬) 적용',
      '모든 자릿수 처리 후 정렬 완료',
    ],
    keyPoints: [
      'O(d·n), d = 자릿수',
      'd가 상수이면 선형 시간',
      '안정적 정렬, 제자리 아님',
    ],
  },
  bucket: {
    concept:
      '값의 범위를 균등하게 나누어 여러 버킷에 분배한 후, 각 버킷을 삽입 정렬로 정렬하고 순서대로 합치는 방식. 균등 분포일 때 선형 시간.',
    steps: [
      '입력값의 범위(MIN~MAX) 계산, 버킷 구간 크기 결정',
      '각 데이터를 해당 버킷에 삽입',
      '각 버킷에 대해 삽입 정렬 수행',
      '버킷 순서대로 데이터를 결과 배열에 나열',
    ],
    keyPoints: [
      '평균 O(n), 최악 O(n²)',
      '입력이 균등 분포일 때 유용',
      '안정적 정렬, 제자리 아님',
    ],
  },
  'sequential-search': {
    concept: '배열의 처음부터 끝까지 하나씩 비교하여 원하는 키 x를 찾는 가장 단순한 탐색 방법.',
    steps: ['i = 0부터 시작', 'A[i]와 x를 비교', '같으면 i 반환, 아니면 i+1로 이동', '끝까지 못 찾으면 -1 반환'],
    keyPoints: ['정렬 불필요', '최선 O(1), 최악 O(n)', '구현이 단순하여 소규모 데이터에 적합'],
  },
  'binary-search': {
    concept: '정렬된 배열에서 중간값과 비교하여 탐색 범위를 반으로 줄여가는 효율적 탐색.',
    steps: ['Left=0, Right=n-1로 초기화', 'Mid = ⌊(Left+Right)/2⌋ 계산', 'A[Mid]와 key 비교', '같으면 반환, 크면 Right=Mid-1, 작으면 Left=Mid+1'],
    keyPoints: ['정렬된 배열 필요', 'O(log n) 시간', '매 단계 탐색 범위 절반 감소'],
  },
  bst: {
    concept: '왼쪽 서브트리 < 루트 < 오른쪽 서브트리 성질을 이용한 이진 트리 기반 탐색 구조.',
    steps: ['루트에서 시작', '키와 현재 노드 비교', '작으면 왼쪽, 크면 오른쪽으로 이동', '찾거나 null에 도달하면 종료'],
    keyPoints: ['평균 O(log n), 최악 O(n) — 편향 트리', '삽입/삭제/탐색 모두 지원', '중위 순회 시 정렬된 결과'],
  },
  'tree-234': {
    concept: '각 노드가 1~3개의 키와 2~4개의 자식을 가지는 균형 탐색 트리. 삽입 시 하향식 분할.',
    steps: ['루트에서 시작', '4-노드를 만나면 중간 키를 부모로 올리고 분할', '적절한 자식으로 이동', '리프에 도달하면 키 삽입'],
    keyPoints: ['항상 균형 유지 — O(log n)', '레드-블랙 트리와 구조적 동치', '하향식(top-down) 삽입으로 구현 단순'],
  },
  'red-black-tree': {
    concept: '각 노드에 빨강/검정 색을 부여하여 균형을 유지하는 이진 탐색 트리. 2-3-4 트리의 이진 표현.',
    steps: ['BST 방식으로 삽입 (새 노드는 빨강)', '부모가 빨강이면 위반 → 수정 필요', '삼촌이 빨강: 재색칠, 삼촌이 검정: 회전+재색칠', '루트를 검정으로'],
    keyPoints: ['모든 연산 O(log n)', '최대 2회 회전으로 삽입 수정 완료', '실무에서 가장 널리 사용되는 균형 트리'],
  },
  'b-tree': {
    concept: '디스크 접근 최적화를 위해 노드 하나에 여러 키를 저장하는 다진 균형 탐색 트리.',
    steps: ['루트에서 시작', '가득 찬 노드를 만나면 분할', '적절한 자식으로 이동', '리프에 키 삽입'],
    keyPoints: ['차수 m인 B-트리: 노드당 최대 m-1개 키', '디스크 I/O 최소화', '데이터베이스 인덱스에 사용'],
  },
  'hash-table': {
    concept: '해시 함수로 키를 테이블 인덱스로 변환하여 평균 O(1) 탐색을 지원하는 자료구조.',
    steps: ['해시 함수로 인덱스 h 계산', '개방 해싱: h번째 체인에 삽입', '폐쇄 해싱: 충돌 시 다음 빈 슬롯 탐사', '선형/이차/이중 해싱으로 탐사'],
    keyPoints: ['평균 O(1), 최악 O(n)', '적재율(load factor)이 성능 결정', '충돌 해결: 개방 해싱(체이닝) vs 폐쇄 해싱(탐사)'],
  },
  dfs: {
    concept: '한 정점에서 출발하여 한 방향으로 가능한 깊이까지 탐색한 후 더 이상 갈 곳이 없으면 되돌아오는 그래프 순회.',
    steps: ['시작 정점을 방문 표시하고 스택에 삽입', '스택에서 정점 추출', '인접한 미방문 정점을 스택에 삽입', '스택이 빌 때까지 반복'],
    keyPoints: ['O(V+E) 시간', '스택 또는 재귀로 구현', '경로 탐색, 사이클 검출에 활용'],
  },
  bfs: {
    concept: '시작 정점에서 가까운 정점부터 먼 정점 순서로 탐색하는 그래프 순회. 레벨별 탐색.',
    steps: ['시작 정점을 방문 표시하고 큐에 삽입', '큐에서 정점 추출', '인접한 미방문 정점을 방문 표시 후 큐에 삽입', '큐가 빌 때까지 반복'],
    keyPoints: ['O(V+E) 시간', '큐로 구현', '최단 경로(가중치 없는 그래프)에 활용'],
  },
  'topological-sort': {
    concept: 'DAG(방향 비순환 그래프)에서 선행 관계를 유지하면서 모든 정점을 일렬로 나열하는 정렬.',
    steps: ['모든 정점의 진입차수 계산', '진입차수 0인 정점을 큐에 삽입', '큐에서 정점 추출 → 결과에 추가', '인접 정점의 진입차수 감소, 0이 되면 큐 삽입'],
    keyPoints: ['O(V+E) 시간', 'DAG에서만 가능 (사이클 존재 시 불가)', '작업 스케줄링, 선수과목 결정에 활용'],
  },
  'connected-components': {
    concept: '무방향 그래프에서 서로 연결된 정점들의 최대 부분 그래프(연결 성분)를 찾는 알고리즘.',
    steps: ['미방문 정점을 찾으면 새 성분 시작', 'DFS/BFS로 연결된 모든 정점 탐색', '탐색된 정점들을 같은 성분으로 그룹화', '모든 정점이 방문될 때까지 반복'],
    keyPoints: ['O(V+E) 시간', 'DFS 또는 BFS 기반', '네트워크 분석, 클러스터링에 활용'],
  },
  kruskal: {
    concept: '간선을 가중치가 작은 순서대로 선택하되, 사이클을 형성하지 않는 간선만 추가하여 MST를 구성하는 탐욕 알고리즘.',
    steps: ['간선을 가중치 오름차순 정렬', '가장 작은 간선 선택', '사이클 형성 여부 확인 (Union-Find)', '사이클 없으면 MST에 추가'],
    keyPoints: ['O(E log E) 시간', '간선 중심 알고리즘', 'Union-Find 자료구조 활용'],
  },
  prim: {
    concept: '하나의 정점에서 시작하여 현재 트리에 인접한 최소 가중치 간선을 반복적으로 추가하며 MST를 확장하는 알고리즘.',
    steps: ['시작 정점 선택, S = {s}', 'S에서 V-S로의 최소 가중치 간선 선택', '해당 정점을 S에 추가, 간선을 MST에 추가', '인접 정점들의 키 값 갱신'],
    keyPoints: ['O(E log V) 시간 (이진 힙)', '정점 중심 알고리즘', '밀집 그래프에 적합'],
  },
  dijkstra: {
    concept: '음이 아닌 가중치 그래프에서 한 출발점으로부터 모든 정점까지의 최단 경로를 구하는 탐욕 알고리즘.',
    steps: ['출발점 d[s]=0, 나머지 d[v]=∞', 'V-S에서 d값이 최소인 정점 u 선택', 'u를 S에 추가', 'u의 인접 정점 v에 대해 d[v] 갱신 (완화)'],
    keyPoints: ['음의 가중치 간선 불허', 'O(E log V) — 우선순위 큐 사용 시', '프림 알고리즘과 구조적 유사성'],
  },
  'bellman-ford': {
    concept: '음의 가중치를 허용하는 단일 출발점 최단 경로 알고리즘. 모든 간선을 V-1회 반복하여 완화.',
    steps: ['d[s]=0, 나머지 d[v]=∞', '|V|-1번 반복: 모든 간선에 대해 완화', 'd[u]+w < d[v]이면 d[v] 갱신', '추가 1회 반복으로 음의 사이클 검사'],
    keyPoints: ['O(VE) 시간', '음의 가중치 간선 허용', '음의 사이클 검출 가능'],
  },
  'floyd-warshall': {
    concept: '모든 정점 쌍 간의 최단 경로를 동적 프로그래밍으로 구하는 알고리즘. 경유 정점을 하나씩 추가.',
    steps: ['D 행렬을 인접 행렬로 초기화', '경유 정점 k = 0부터 n-1까지', '각 (i,j) 쌍에 대해 k를 경유하는 경로와 비교', 'D[i][k]+D[k][j] < D[i][j]이면 갱신'],
    keyPoints: ['O(V³) 시간', '음의 가중치 허용 (음의 사이클 제외)', '구현이 간단, 밀집 그래프에 적합'],
  },
  'ford-fulkerson': {
    concept: '잔여 그래프에서 증가 경로를 반복적으로 찾아 유량을 추가하여 네트워크의 최대 유량을 구하는 알고리즘.',
    steps: ['모든 간선의 유량을 0으로 초기화', 'BFS로 s에서 t까지의 증가 경로 탐색', '경로의 최소 잔여 용량 δ 계산', '경로의 간선 유량 += δ, 역방향 -= δ'],
    keyPoints: ['최대 유량 = 최소 컷 (최대 유량-최소 컷 정리)', 'BFS 사용 시 O(VE²) — 에드몬즈-카프', '네트워크 설계, 이분 매칭에 활용'],
  },
};
